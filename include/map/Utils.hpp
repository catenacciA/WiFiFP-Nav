#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

using namespace std;

// Function declaration
vector<string> split(const string &str, char delimiter);

size_t reduceHashValue(size_t hashValue);

#endif // UTILS_H
